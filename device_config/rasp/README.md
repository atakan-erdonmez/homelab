These are specific configs for the physical device, Raspberry Pi.

Note: You should add the following line to .bashrc manually for Ansible to work consistently:
ANSIBLE_CONFIG=/home/atakan/homelab/ansible.cfg

