services:
  management-proxy:
    image: nginx:alpine
    container_name: management-proxy
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro 
